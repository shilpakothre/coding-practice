package com.example.coding_practice.part1;

import java.util.Arrays;
import java.util.List;
import java.util.function.Function;
import java.util.stream.Collectors;

public class Test2 {
    static void main() {
        /*  write a program to count vowels in List of String*/

        List<String> a = Arrays.asList("apple", "banana", "cherry", "orange", "lemon", "grape", "lemon", "pineapple");

        //  a.stream().collect(Collectors.groupingBy(Function.identity(),Collectors.filtering(e->e.))
    }
}
